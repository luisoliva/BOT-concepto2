import { RfcDirectiveDirective } from './rfc-directive.directive';

describe('RfcDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RfcDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
