import { ListValidationDirective } from './list-validation.directive';

describe('ListValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ListValidationDirective();
    expect(directive).toBeTruthy();
  });
});
